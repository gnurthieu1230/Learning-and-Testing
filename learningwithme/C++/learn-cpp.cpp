### Các kí tự viết tắt hàm và biến trong C++ ###

# 1. Các kiểu dữ liệu cơ bản (Basic Data Types)
int = Integer
float = floating-point
double = double precision
char = Character
bool = boolean
void = empty

# 2. Chuỗi & Kiểu dữ liệu từ thư viện chuẩn (Standard Library Types)
std::string = std là standard (standard::string)
auto = auto

# 3. Hàm trong C++ (Functions)
main() = main [hàm chính]
return = return [hàm trả về]

# 4. Từ khóa bổ trợ cho Biến (Variable Modifiers & Specifiers)
const = Constant 
constexpr = Constant Expression 
static = Static
unsigned = Unsigned
signed = Signed
short / long = Short / Long Integer

# 5. Nhập / Xuất dữ liệu cơ bản (Basic I/O) [Dùng thư viện upstream]
cin = Character Input
cout = Character Output
cerr = Character Error
endl = End Line


