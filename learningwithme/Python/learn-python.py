### TÊN VIẾT TẮT VÀ ĐẦY ĐỦ TRONG PYTHON ###

# 1. Biến & Từ khóa (Variables & Keywords)
var = variable
def = define
elif = else if

# 2. Kiểu dữ liệu (Data Types)
int = integer
float = floating-point number
complex = complex number
str = string
bool = boolean
list = list
tuple = tuple
dict = dictionary
set = set
frozenset = frozen set
bytes = bytes
bytearray = byte array
memoryview = memory view
NoneType = none type

# 3. Hàm toán học & Xử lý cơ bản
abs() = absolute
len() = length
max() / min() = maximum / minimum
pow() = power
divmod() = division & modulus
ord() = ordinal
chr() = character
ascii() = ASCII
repr() = representation

# 4. Hàm kiểm tra & Thao tác trên đối tượng
isinstance() = is instance
issubclass() = is subclass
getattr() = get attribute
setattr() = set attribute
delattr() = del attribute
hasattr() = has attribute

# 5. Hàm chuyển đổi hệ cơ số
bin() = binary
hex() = hexadecimal
oct() = octal

# 6. Hàm lặp & Thực thi
iter() = iterator
next() = next
eval() = evaluate
exec() = execute

# 7. Hàm Quản lý và Xuất/Nhập file
open() = open
read() = read
readline / readlines = Read Line / Read Lines
write / writelines = Write / Write Lines
input() = input
print() = print

# 8. Hàm thao tác với thuộc tính và Phương thức (Advance)
callable() = callable
classmethod() = Class Method
staticmethod() = Static Method
super() = Super Class

# 9. từ khóa viết tắt điều khiển & cấu trúc
pass = Pass
break = Break
cont / continue = Continue
del = Delete
assert = Assert
try / except = Try / Exception
raise = Raise

### Toán Tử (Operators) ###
# 10. Toán tử số học (Arithmetic Operators)
+ = addition
- = subtraction
* = Multiplication
/ = Division
// = Floor Division
% = Modulo / Modulus
** = Exponentiation

# 11. Toán tử so sánh (Comparison / Relational Operators)
== = Equal to
!= = Not Equal to
>/< = Greater than / less than
>= = Greater than or Equal to
<= = Less than or Equal to

# 12. Toán tử gán kết hợp (Augmented Assignment Operators)
= = Assignment
+= = Plus Equals
-= = Minus Equals
*= = Multiply Equals
/= = Divide Equals
//= = Floor Divide Equals
%= = Modulo Equals
**= = Exponent Equals

# 13. Toán tử Logic & Định danh (Logical, Identity & Membership Operators)
and = Logical AND
or = Logical OR
not = Logical NOT
is = Identity (Is)
is not = Identity (Is Not)
in = Membership (In)
not in = Membership (Not In)

# 14. Toán tử Bitwise (Thao tác trên bit nhị phân)

